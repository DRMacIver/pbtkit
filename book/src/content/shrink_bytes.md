<div class="ai">

*The following summary was written by an LLM.*

The advanced bytes passes add byte-string-specific shrinking on top of the generic sequence passes. They exploit the length-prefixed structure of byte strings to redistribute bytes between adjacent byte-string draws, and to try shortening one byte string while lengthening another when the total size matters.

</div>
